<Window x:Class="OSA_File_Management_System.View.MonitoringView.AddMonitoringForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OSA_File_Management_System.View.MonitoringView"
        Title="Add Monitoring Entry" Height="650" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Document Type:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding SelectedDocumentType, Mode=OneWay}" IsReadOnly="True" 
                 Margin="0,0,0,15" Height="30" FontSize="14" Background="#F0F0F0"/>

        <TextBlock Grid.Row="2" Text="Municipality:" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="3" x:Name="MunicipalityComboBox" ItemsSource="{Binding MunicipalityList}" SelectedItem="{Binding AddFormData.Municipality}" 
                  Margin="0,0,0,15" Height="30" FontSize="14" SelectionChanged="MunicipalityComboBox_SelectionChanged"/>

        <TextBlock Grid.Row="4" Text="Barangay:" FontWeight="Bold" Margin="0,0,0,5" x:Name="BarangayLabel" 
                   Visibility="{Binding IsBarangayDocumentType, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <ComboBox Grid.Row="5" x:Name="BarangayComboBox" ItemsSource="{Binding BarangayList}" SelectedItem="{Binding AddFormData.Barangay}" 
                  Margin="0,0,0,15" Height="30" FontSize="14"
                  Visibility="{Binding IsBarangayDocumentType, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <TextBlock Grid.Row="6" Text="Year:" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="7" ItemsSource="{Binding AddFormYearList}" SelectedItem="{Binding AddFormYear}"
                  Margin="0,0,0,15" Height="30" FontSize="14"/>

        <TextBlock Grid.Row="8" Text="Date Submitted:" FontWeight="Bold" Margin="0,0,0,5"/>
        <DatePicker Grid.Row="9" SelectedDate="{Binding AddFormData.DateSubmitted}" Margin="0,0,0,15" Height="30"/>

        <TextBlock Grid.Row="10" Text="Status:" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="11" x:Name="StatusComboBox" Margin="0,0,0,0" Height="30" FontSize="14"
                  SelectedValue="{Binding AddFormStatus}"
                  SelectedValuePath="Content">
            <ComboBoxItem Content="Submitted"/>
            <ComboBoxItem Content="Not Submitted"/>
        </ComboBox>

        <TextBlock Grid.Row="12" Text="Remarks:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="13" Text="{Binding AddFormData.Remarks}" Margin="0,0,0,15" 
                 Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="14" Text="PDF Link (Google Drive/OneDrive):" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="15" Text="{Binding AddFormData.PdfLink}" Margin="0,0,0,15" 
                 Height="40" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="16" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding AddMonitoring}" 
                    Width="100" Height="35" Margin="0,0,10,0"
                    Background="#257180" Foreground="White" FontWeight="Bold" BorderThickness="0">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="5"/></Style></Button.Resources>
            </Button>
            <Button Content="Cancel" Command="{Binding CloseAddForm}" 
                    Width="100" Height="35"
                    Background="#6c757d" Foreground="White" FontWeight="Bold" BorderThickness="0">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="5"/></Style></Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>